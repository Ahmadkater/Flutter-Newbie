import 'package:flutter/material.dart';

class Hadiethscreen extends StatelessWidget {
  const Hadiethscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      color: Colors.red,
    );
  }
}
